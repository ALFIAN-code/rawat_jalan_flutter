import 'package:flutter/widgets.dart';

class KelolaPasienPage extends StatelessWidget {
  const KelolaPasienPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ini adalah page kelola pasien'),
    );
  }
}